# Use the official Open WebUI as the base image
FROM ghcr.io/open-webui/open-webui:0.6.34

# Copy the custom branding CSS file into the container.
# This will apply the 'CorpChat' theme.
COPY ./branding/corpchat.css /app/backend/static/css/themes/corpchat.css
